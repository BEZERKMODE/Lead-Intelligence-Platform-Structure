version: '3.8'

services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: lead_intel_db
    environment:
      POSTGRES_USER: lead_intel_user
      POSTGRES_PASSWORD: supersecurepassword123
      POSTGRES_DB: lead_intel_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    restart: unless-stopped

  redis:
    image: redis:alpine
    container_name: lead_intel_redis
    ports:
      - "6379:6379"
    restart: unless-stopped

  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: lead_intel_pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@kinstechnology.com
      PGADMIN_DEFAULT_PASSWORD: supersecurepassword123
      PGADMIN_SERVER_JSON_FILE: /pgadmin4/servers.json
    ports:
      - "5050:80"
    volumes:
      - ./postgres/servers.json:/pgadmin4/servers.json:ro
    depends_on:
      - postgres
    restart: unless-stopped

  prometheus:
    image: prom/prometheus:latest
    container_name: lead_intel_prometheus
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
    ports:
      - "9090:9090"
    restart: unless-stopped

  grafana:
    image: grafana/grafana:latest
    container_name: lead_intel_grafana
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
    ports:
      - "3000:3000"
    depends_on:
      - prometheus
    restart: unless-stopped

volumes:
  pgdata:
